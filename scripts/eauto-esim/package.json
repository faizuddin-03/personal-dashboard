{
  "name": "eauto-esim",
  "version": "1.0.0",
  "description": "eAuto Simulator (eSIM) — edit simulator response records by vehicle prefix",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
