# Injected by app/api/eauto-esim/run/route.ts. Listed for standalone debugging.
# The eSIM URL is fixed in data/config.ts and deliberately not overridable.
ESIM_USER=admin
ESIM_PASS=admin
# estm-enquiry | estm-submission
ESIM_ENTITY=estm-enquiry
ESIM_PREFIX=
# JSON of form-label -> new value. Blank/absent means leave that field alone.
ESIM_CHANGES={"Response Code":"VEL000069E"}
ESIM_HEADLESS=
