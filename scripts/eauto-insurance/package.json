{
  "name": "eauto-insurance-checker",
  "version": "1.0.0",
  "description": "Automated insurance quote checker for eAuto staging back office",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "install-browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "@playwright/test": "^1.48.0",
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
